{% if matchs|length > 0 %}

    <table class="table table-striped table-bordered">
    <tr><th>Équipe</th><th>Score</th><th>Équipe</th>{% if removeTab is not defined %}<th>TAB</th>{% endif %}{% if removeForfait is not defined %}<th>Forfait</th>{% endif %}<th>Horaire</th>{% if removeField is not defined %}<th>Terrain</th>{% endif %}</tr>
    {% for meet in matchs %}
                {% include "admin/block/displayMatch.html.twig" %}





    {% endfor %}    </table> {% endif %}