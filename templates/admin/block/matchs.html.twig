{% if phase.meets|length > 0 %}

{% for key, group in phase.meets|filter(v => v.poule.id == groupe)|group_by(==> _.tour) %}
    <h5 class="mt-4">Tour {{ key }}</h5>
    <table class="table table-striped table-bordered">
        <tr><th>Équipe</th><th>Score</th><th>Équipe</th>{% if removeTab is not defined %}<th>TAB</th>{% endif %}{% if removeForfait is not defined %}<th>Forfait</th>{% endif %}<th>Horaire</th>{% if removeField is not defined %}<th colspan="3">Terrain</th>{% endif %}</tr>
        {% for meet in group %}
           {% include "admin/block/displayMatch.html.twig" %}

        {% endfor %}

    </table>


{% endfor %}    {% endif %}